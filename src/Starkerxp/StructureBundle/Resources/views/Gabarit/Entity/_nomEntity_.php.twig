<?php

namespace {{ namespaceEntity }}\Entity;

use Doctrine\ORM\Mapping as ORM;
use Starkerxp\StructureBundle\Entity\UserEntity;

/**
 * {{ nomEntity }}
 *
 * @ORM\Table(name="{{ nomEntity|lower }}", indexes={
 *  @ORM\Index(columns={"created_at"}),
 *  @ORM\Index(columns={"updated_at"})
 * })
 * @ORM\Entity(repositoryClass="{{ namespaceEntity }}\Repository\{{ nomEntity }}Repository")
 */
class {{ nomEntity }} extends UserEntity
{

}
