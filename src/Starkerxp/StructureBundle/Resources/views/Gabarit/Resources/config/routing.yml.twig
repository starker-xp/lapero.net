{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.cget:
    path:     /
    defaults: { _controller: "{{namespaceController}}:{{ nomController }}:cget" }
    methods:  [GET]

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.get:
    path:     /{id}
    defaults: { _controller: "{{namespaceController}}:{{ nomController }}:get" }
    methods:  [GET]
    requirements:
        page: '\d+'

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.post:
    path:     /
    defaults: { _controller: "{{namespaceController}}:{{ nomController }}:post" }
    methods:  [POST]

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.put:
    path:     /{id}
    defaults: { _controller: "{{namespaceController}}:{{ nomController }}:put" }
    methods:  [PUT]
    requirements:
        page: '\d+'

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.delete:
    path:     /{id}
    defaults: { _controller: "{{namespaceController}}:{{ nomController }}:delete" }
    methods:  [DELETE]
    requirements:
        page: '\d+'
