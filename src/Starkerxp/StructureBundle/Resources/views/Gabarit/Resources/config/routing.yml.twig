{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.cget:
    path:     /{{ nomController|lower }}s
    defaults: { _controller: "{{namespaceControllerBundle}}:{{ nomController }}:cget" }
    methods:  [GET]

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.get:
    path:     /{{ nomController|lower }}s/{id}
    defaults: { _controller: "{{namespaceControllerBundle}}:{{ nomController }}:get" }
    methods:  [GET]
    requirements:
        page: '\d+'

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.post:
    path:     /{{ nomController|lower }}s
    defaults: { _controller: "{{namespaceControllerBundle}}:{{ nomController }}:post" }
    methods:  [POST]

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.put:
    path:     /{{ nomController|lower }}s/{id}
    defaults: { _controller: "{{namespaceControllerBundle}}:{{ nomController }}:put" }
    methods:  [PUT]
    requirements:
        page: '\d+'

{{ nomServiceController }}.controller.{{ nomControllerCamelize|lower }}.delete:
    path:     /{{ nomController|lower }}s/{id}
    defaults: { _controller: "{{namespaceControllerBundle}}:{{ nomController }}:delete" }
    methods:  [DELETE]
    requirements:
        page: '\d+'
